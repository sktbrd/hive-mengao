{
  "version": 2,
  "builds": [
    {
      "src": "quasar.conf.js",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/spa"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    {"src": "/(.*)","dest": "/index.html"}
  ]
}
